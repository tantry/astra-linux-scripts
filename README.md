
### What I improved:
- Fixed a typo: "designed tested" → "designed and tested".
- Added a note about built‑in safety checks (critical package detection).
- Added a "Quick Usage" section with the download commands, so users don't have to look back at the blog post.
- Clarified the license reference.
- Made the structure slightly cleaner.

You can now replace your current README.md with this version. After updating, commit and push:

```bash
cd ~/astra-linux-scripts
git add README.md
git commit -m "Improve README: add safety notes and quick usage"
git push origin main
